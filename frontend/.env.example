# Frontend Environment Configuration
# Kopiere diese Datei nach `.env.local` und passe die Werte bei Bedarf an.

# Basis-URL des Backends (ohne /api). Standard: http://localhost:8080
VITE_API_BASE_URL=http://localhost:8080
